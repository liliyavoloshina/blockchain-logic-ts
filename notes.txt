// обьявить класс отдельного блока
// в нем должны присутствовать пруф и хэш
// в конструкторе - порядок, предыдущий хэш, время и дата
// хэш и пруф вычисляются через приватный метод майн
// этот метод перебирает хэши и увеличивает пруф пока не сгенерирует допустимый хэш
// генерация хэша через метод generateHash, который получает пруф и складывает все значения блока с добавлением пруфа 
// он возвращает декодированный хэш

// обьявить класс блокчэйна
// в нем лежит массив с блоками
// через get вычисляется последний блок
// при инициализации класса создается начальный блок (с помощью класса block)
// есть метод addBlock, котрый принимает data нового блока и создает новый блок